import { UselessDirective } from './useless.directive';

describe('UselessDirective', () => {
  it('should create an instance', () => {
    const directive = new UselessDirective();
    expect(directive).toBeTruthy();
  });
});
